<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Язык админки
    |--------------------------------------------------------------------------
    |
    | Локаль применяется только к запросам панели Filament, витрина
    | продолжает работать с APP_LOCALE.
    |
    */

    'locale' => env('ADMIN_LOCALE', 'ru'),

    /*
    |--------------------------------------------------------------------------
    | Доступ в панель
    |--------------------------------------------------------------------------
    |
    | Список e-mail через запятую в ADMIN_EMAILS. Если список пуст, в панель
    | может войти любой пользователь из таблицы users.
    |
    */

    'emails' => array_values(array_filter(array_map(
        'trim',
        explode(',', (string) env('ADMIN_EMAILS', '')),
    ))),

    /*
    |--------------------------------------------------------------------------
    | Импорт товаров
    |--------------------------------------------------------------------------
    |
    | Файл нарезается на порции по chunk_size строк, каждая порция уходит
    | отдельной задачей в очередь. Каталоги в directories сканируются для
    | режима «файл на сервере» — так можно импортировать файлы, которые
    | не проходят через upload_max_filesize.
    |
    */

    'import' => [
        'chunk_size' => (int) env('PRODUCT_IMPORT_CHUNK_SIZE', 50),

        'queue' => env('PRODUCT_IMPORT_QUEUE'),

        'directories' => [
            storage_path('app/imports'),
            storage_path('app/private/imports'),
            public_path('storage'),
        ],

        /* Сколько последних задач показывать в панели задач админки. */
        'history' => 15,
    ],

];
